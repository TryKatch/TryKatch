# Enterprise foundation — evidence and remaining gates

Reconciled: 2026-09-17. Release baseline: **Trykatch 0.1.0-preview.25**, commit `6e1b7d40dd7b0ac985c5046584be98bebd0c8c9a`.

This ledger reconciles the [hardening backlog](plans/enterprise-foundation-hardening-plan.md) with shipped changes. It does not certify a hosting environment, replace the [production-readiness policy](production-readiness-plan.md), or authorize a stable release. An unchecked row means its complete acceptance contract is not yet evidenced; it does not prove that every control in that row is missing or broken.

## Verified release baseline

- [Published preview.25 release](https://github.com/TryKatch/TryKatch/releases/tag/v0.1.0-preview.25) and [release workflow](https://github.com/TryKatch/TryKatch/actions/runs/35220550730): successful. Both `Trykatch.Templates` and `Trykatch.Cli` were published.
- [Main CI at the release commit](https://github.com/TryKatch/TryKatch/actions/runs/35220515602): all 17 jobs successful, including real PostgreSQL integration, generated production containers/browser journeys, artifact-boundary probes, frontend tests, architecture, supply-chain and template matrices.
- The `verify-backend` log records **282 unit tests and 236 integration tests, zero failures and zero skips**. These counts refer to those two source suites, not the sum of all module/generated suites.
- Each original fix commit listed below was verified as an ancestor of `v0.1.0-preview.25`. The reconciliation branch starts at `463bb22f8f3e81c245649356488441f402018f33`; its initial tracked tree matches the released commit. Documentation-only edits do not constitute a new application qualification run.
- [Tracking issue #64](https://github.com/TryKatch/TryKatch/issues/64) and [foundation promotion #71](https://github.com/TryKatch/TryKatch/pull/71) retain the historical implementation/review records. Historical Astra reviews are not a fresh integrated release-candidate review.

**Released** below means the accepted foundation fix is merged, covered by the cited tests in the release baseline, and included in the published preview. **Partial** means some implementation/evidence exists but the complete original acceptance criteria remain open. Deployment-specific qualification is separate even for released controls.

## Finding-to-PR ledger

Test links are repository-relative navigation aids. The immutable release commit and CI run above identify the actual code that was executed.

| ID | Status | Shipped implementation and verification | Remaining qualification or closure requirement |
| --- | --- | --- | --- |
| 01 | Released | [#62](https://github.com/TryKatch/TryKatch/pull/62), `77d86690c18b31b449513fc3e97a62caf6516ea6`; [platform access tests](../templates/trykatch/tests/Trykatch.UnitTests/PlatformAccessTests.cs) and [existing/fresh-session suspension tests](../templates/trykatch/tests/Trykatch.IntegrationTests/AuthenticationSecurityTests.cs). | Repeat the deployed identity matrix during final product UAT; platform suspension and organization membership remain separate authorities. |
| 02 | Released | [#66](https://github.com/TryKatch/TryKatch/pull/66), `c73794ce83cb1e3e889b382fbe881f7841c9db66`; [target privilege and final-administrator tests](../templates/trykatch/tests/Trykatch.IntegrationTests/AccessManagementBoundaryTests.cs). | General record-version concurrency is a separate requirement in 08. |
| 03 | Released | [#67](https://github.com/TryKatch/TryKatch/pull/67), `35cde5af2c751adde8d06c17e4e47ba2c9247bd5`; [session/purpose-bound, single-use assurance and MFA tests](../templates/trykatch/tests/Trykatch.IntegrationTests/AccountSecurityStepUpTests.cs). | Full manual accessibility and deployed MFA journeys remain in 13/15. |
| 04 | Released | [#63](https://github.com/TryKatch/TryKatch/pull/63), `bd818ec3fdc2d494ce6641ff0685bdbbba0a5d5c`; [source, NuGet and Docker sentinel probes](../scripts/test-artifact-boundaries.sh) executed with `--runtime` in `verify-package`, alongside release-baseline security tests. | Product-added adapters, ingress and diagnostics must preserve these exclusions; this is not a claim that arbitrary downstream payloads are safe. |
| 05 | Released | [#68](https://github.com/TryKatch/TryKatch/pull/68), `c5662a48f31a39b6cdb58678db72dab940a5fd65`; [encrypted key repository](../templates/trykatch/tests/Trykatch.IntegrationTests/ProductionKeyRepositoryTests.cs), [identity continuity/rotation](../templates/trykatch/tests/Trykatch.IntegrationTests/ProductionIdentityContinuityTests.cs), [startup configuration](../templates/trykatch/tests/Trykatch.IntegrationTests/ProductionIdentityConfigurationTests.cs) and [SMTP TLS](../templates/trykatch/tests/Trykatch.IntegrationTests/SmtpTransportTests.cs) tests. | Actual Linux secret mounts, replica continuity, certificate ownership and timed backup/PITR recovery still require deployment evidence in 15. |
| 06 | Released | [#69](https://github.com/TryKatch/TryKatch/pull/69), `99f0df8ffc2ae922b7ed1eef5b423892cd20c111`; [commit-before-response](../templates/trykatch/tests/Trykatch.IntegrationTests/AtomicMutationResponseIntegrationTests.cs) and [business/audit-intent atomicity](../templates/trykatch/tests/Trykatch.IntegrationTests/AuditIntentAtomicityTests.cs) tests. | Accepted foundation scope does not close invitation coordination or every ambiguous-disconnect/idempotency case in 08; deployed projection capacity/retention belongs to 15. |
| 07 | Released | [#70](https://github.com/TryKatch/TryKatch/pull/70), `9e6ab19b1d6f27f5fe2aa547bc9156ab5096d89c`; [database outage, fresh scopes, replay, restart and at-least-once recovery tests](../templates/trykatch/tests/Trykatch.IntegrationTests/OutboxRecoveryIntegrationTests.cs). | No exactly-once external-delivery guarantee. Measure lock duration, sustained recovery, alert delivery and consumer behavior for the real workload in 15. |
| 08 | Partial | Final-administrator concurrency is covered by 02. [Platform users](../templates/trykatch/src/API/Trykatch.Api/Controllers/PlatformUsersController.cs) and [organizations](../templates/trykatch/src/API/Trykatch.Api/Controllers/OrganizationsController.cs) already have bounded pages; preview.25 also qualifies generated business blueprints. | Inventory all mutable administration/business records; finish observed-version update/delete contracts and conflict UX, invitation acceptance/cancellation race tests, stable database sorting and large-tenant query evidence. Existing paging is not proof of this whole gate. |
| 09 | Partial | [Observability contracts](../templates/trykatch/tests/Trykatch.IntegrationTests/ObservabilityContractTests.cs) and [collector configuration/durable-queue runtime checks](../templates/trykatch/scripts/test-observability.sh) passed. | [Request logging](../templates/trykatch/src/Common/Trykatch.ServiceDefaults/Observability/SafeRequestLoggingMiddleware.cs) still looks for `RouteEndpoint` in metadata rather than inspecting the endpoint instance. Complete route/exception/abort/privacy tests and real request-to-log/trace/metric ingestion plus alert delivery. |
| 10 | Partial | [#61](https://github.com/TryKatch/TryKatch/pull/61), `cb385177501b38c27ff558562ec84e491a2369e3`, shipped generated-root pnpm resolution/pinning. Main CI passed difficult-name/template matrices and [generated production qualification](generated-application-qualification.md). | Issue #64 marks that implemented core complete, not the entire original requirement. Preview.25 generated workflows still used floating action tags and NuGet caches keyed by excluded locks, and lacked a generated SDK policy. SDK/workflow corrections are part of the unreleased work below. Complete the locked-restore transition, clean Windows/macOS/Linux first CI and CLI/AppHost startup plus separate Rider/Visual Studio checks. |
| 11 | Partial | Change-detection tests, pinned actions, protected-main tag ancestry, release-version contract, SBOM and NuGet trusted publishing exist. Preview.25 release succeeded. | Build API and regenerate contracts in the same drift-check workspace. Qualify and publish the exact same downloaded package bytes, with commit/test/digest binding and negative tests. Require explicit stable approval; passing rebuilt-package qualification alone is insufficient. |
| 12 | Released | [#65](https://github.com/TryKatch/TryKatch/pull/65), `5de2c4220f885bc29919cf1792e45f84d1edebc3`; [supported placement and fail-closed tests](../templates/trykatch/tests/Trykatch.IntegrationTests/OrganizationDataPlacementTests.cs). | Dedicated-database tenancy remains unavailable until separately implemented and qualified; extension interfaces are not an operational capability. |
| 13 | Partial | Production proxy/header checks and automated frontend/generated browser journeys passed in release CI. | Record actual-bundle CSP behavior, all specified identity/administration/project/document failure paths, desktop/mobile accessibility, manual keyboard/screen-reader checks and required English/French key completeness. Existing journeys are not full final UAT. |
| 14 | Partial | Feature-boundary work, architecture tests and developer onboarding shipped; this reconciliation supplies the missing finding-to-PR documentation ledger. | Complete remaining feature-seam/lifetime/async review and exercised bilingual operational/upgrade instructions. Documentation reconciliation alone does not close the refactoring or dependency gates. |
| 15 | Open | Integration tests establish fault-handling behavior, not an accepted deployment capacity or disaster-recovery envelope. | Record versioned migration/failure/restore/PITR/key-rotation/outage/shutdown drills and workload/resource measurements. Maintainers must accept measured SLO/RTO/RPO and CI thresholds; do not invent them. |
| 16 | Open | Preview.25 public publication and prior independent foundation reviews are evidence components only. | After 01–15 pass, obtain fresh integrated independent review and operational/UAT sign-off; qualify public RC installation on clean machines and attach checksums/SBOM/provenance. Stable promotion remains a separate explicit decision. |

## Production-readiness phase reconciliation

No overall production phase is marked complete merely because the source CI is green.

| Phase | Evidence available | Still needed before phase closure |
| --- | --- | --- |
| 0 — Governance | Default `develop`, protected `main`, promotion PR and main-contained tag, trusted NuGet publishing. | `develop` protection is absent: the branch-protection API returned HTTP 404 on 2026-09-17. Review and enforce the documented protection/review policy; no repository permissions were changed during this reconciliation. |
| 1 — Product/local correctness | Source/web/generated suites and fresh generated production journey. | Explicit inventory of all operational surfaces and deliberate loading/empty/forbidden/degraded states; complete option-combination acceptance. |
| 2 — Generated application | Default React production runtime; backend/web first-feature and blueprint PostgreSQL qualification; packaging matrices. | Full runtime/browser qualification of every supported optional permutation. Build/Compose validation alone does not close this requirement. |
| 3 — Containers/network | Production API/web images, proxy headers, Compose and default browser journeys. | Complete target-deployment policy, secret/filesystem/resource/shutdown and network evidence across supported modes. |
| 4 — Migration/recovery | Runtime-role/RLS tests and key persistence/continuity tests. | Previous-supported-schema upgrade/failure drills and encrypted timed restore/PITR evidence with accepted recovery objectives. |
| 5 — Observability | Collector configuration, queue and telemetry-contract checks. | Follow a real request through configured logs/traces/metrics; demonstrate outage resilience and actionable delivered alerts. |
| 6 — Security | Released 01–07/12 controls, PostgreSQL isolation, dependency/license/SBOM/secret checks. | Finish 08/09/11/13, deployment rotation/identity matrix and integrated threat-model review. |
| 7 — Capacity | Bounded pages/retries and focused failure tests. | Versioned load profiles, hardware/dataset/concurrency evidence, accepted latency/throughput/pool/error budgets and noisy-neighbor drills. |
| 8 — Final UAT/release | Historical focused browser UAT and public preview packages. | Full accessibility/product UAT, clean IDE/OS public-install records, independent review, approved RC and explicit stable sign-off. |

## Closure order and evidence rules

1. Finish engineering gaps 08/09/11/13 and the remaining scope of 10/14. Correct repository governance through an approved policy change, not by rewriting the requirement.
2. Qualify a named disposable deployment using recorded artifact versions. A local Docker drill is useful baseline evidence, not proof for an unspecified production host.
3. Run 15 with measured resources/workloads; obtain maintainer acceptance of budgets and recovery objectives.
4. Complete the independent review and public RC/UAT process in 16. Only then consider a separately approved stable release.

For each new closure, record the requirement, commit/PR, exact command or CI run, executed/skipped tests, qualified artifact identity, environment, result and remaining limitations. Re-run affected evidence when application/package inputs change. Preserve dated historical evidence instead of relabeling it as a fresh run. No new release, production drill, capacity commitment or independent approval is implied by this documentation reconciliation.

## Unreleased engineering follow-through

The [implementation/verification record](specs/production-readiness-gates.md) tracks subsequent code changes in 09/10/11 separately from the published preview.25 baseline:

- Request logging now reads the endpoint instance and records one safe completion on normal, exceptional and aborted paths; the actual privacy sanitizer recognizes its bounded fields/template.
- Source API-only changes select the web check, which builds OpenAPI before regenerating/comparing clients in the same checkout.
- Release candidates are packed once and consumed through validated prepared-package harnesses. Publication downloads a specific artifact and checks digest/size/version/commit/run/source-CI binding before obtaining a credential. Candidates and missing qualification are rejected.
- Stable publication remains fail-closed while hardening boxes remain open, and additionally requires configured environment reviewers and their approval. Preview publishing retains its existing environment-free identity. GitHub/NuGet policy configuration is not changed here; [NuGet's trusted-publishing contract](https://learn.microsoft.com/en-us/nuget/nuget-org/trusted-publishing) documents the environment binding.
- Generated projects now receive a patch-roll-forward SDK policy matching the tested feature band, pinned workflow actions and first-restore-safe cache configuration. Source/generated CI SDK selection is aligned.

These are local implementation changes, not newly released controls. Full gate 09 still needs end-to-end ingestion/alert proof; 10 still needs locked-restore and OS/IDE qualification; 11 still needs integrated workflow/transfer evidence and an approved release. Other ledger rows remain unchanged.
