# Flatpack Federation

Optional generic OpenID Connect federation module. It is disabled by default and must pass provider discovery testing before a connection can be enabled. Provider secrets are write-only and protected by the host Data Protection key ring.
