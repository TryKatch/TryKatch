import type { FlatpackWebOverrides } from '@flatpackapp/module-sdk'

/** Application-owned overrides. Generated module registries import this file but never rewrite it. */
export const workspaceOverrides = {} satisfies FlatpackWebOverrides
